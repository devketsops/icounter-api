Metrics Server has been deployed successfully!

To verify metrics-server is running:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name=metrics-server

To check node metrics:
  kubectl top nodes

To check pod metrics:
  kubectl top pods --all-namespaces
