environment: production
replicaCount: 3

config:
  nodeEnv: production
  logLevel: warn

autoscaling:
  minReplicas: 3
  maxReplicas: 10

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
