icounter-api has been deployed successfully!

To get the ALB endpoint:
  kubectl get ingress {{ include "app.name" . }} -n {{ .Release.Namespace }}

To check pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l app={{ include "app.name" . }}

To check HPA status:
  kubectl get hpa {{ include "app.name" . }} -n {{ .Release.Namespace }}
