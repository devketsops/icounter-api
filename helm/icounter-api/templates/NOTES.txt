icounter-api has been deployed successfully!

To get the ALB endpoint:
  kubectl get ingress {{ include "app.name" . }} -n {{ .Release.Namespace }}

To check pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l app={{ include "app.name" . }}

To check HPA status:
  kubectl get hpa {{ include "app.name" . }} -n {{ .Release.Namespace }}
{{- if .Values.networkPolicy.enabled }}

NetworkPolicy is enabled. Ensure the VPC CNI addon has ENABLE_NETWORK_POLICY=true.
Allowed ingress CIDRs: {{ .Values.networkPolicy.ingress.fromCIDR }}
  kubectl get networkpolicy {{ include "app.name" . }} -n {{ .Release.Namespace }}
{{- end }}
{{- if .Values.podDisruptionBudget.enabled }}

PodDisruptionBudget is active (minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}).
  kubectl get pdb {{ include "app.name" . }} -n {{ .Release.Namespace }}
{{- end }}
